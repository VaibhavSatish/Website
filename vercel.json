{
  "buildCommand": "git config extraheader \"AUTHORIZATION: basic $(echo -n x-access-token:$GITHUB_ACCESS_TOKEN | base64)\" && git submodule update --init --recursive",
  "routes": [
    {
      "src": "/resumes/(.*)",
      "status": 403
    }
  ]
}